rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the sparkstock collection is used
    match /sparkstock/{document} {
      allow read: if true;        // Anyone can view inventory
      allow write: if true;       // Password protection is handled in the app UI
    }
  }
}
